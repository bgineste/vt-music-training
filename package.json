{
	"name": "vt-music-training",
	"version": "0.1.0",
	"description": "Example block scaffolded with Create Block tool.",
	"author": "The WordPress Contributors",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "wp-scripts build --blocks-manifest",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "wp-scripts plugin-zip",
		"start": "wp-scripts start --blocks-manifest",
		"dist": "node ../common-scripts/scripts/make-dist.js",
		"upload": "node ../common-scripts/scripts/upload-dist.js",
		"deploy": "npm run dist && npm run upload",
		"upload-local": "node ../common-scripts/scripts/upload-local.js",
		"deploy-local": "npm run dist && npm run upload-local"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.19.0",
		"basic-ftp": "^5.0.5",
		"dotenv": "^17.0.1",
		"fs-extra": "^11.3.0"
	},
	"dependencies": {
		"sweetalert2": "^11.22.2"
	}
}
